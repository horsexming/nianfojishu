package com.task.Server;

import java.util.Map;

import com.task.entity.Renew;
import com.task.entity.Store;


public interface IRenewService {
	void add(Renew re);
	void del(Renew re);
	String update(Renew re);
	Renew getRenewById(int id);
	Object[] queryRenew(Map map, int pageNo, int pageSize);
	String add(Renew re,Integer storeId);
	Store getStoreById(Integer id);
	void delRenewById(Integer id);
	
}
