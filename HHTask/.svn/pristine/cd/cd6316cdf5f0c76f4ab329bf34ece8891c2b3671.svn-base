package com.task.Server;

import java.util.List;

import com.task.entity.android.InsScope;
import com.task.entity.android.InsTemplate;

public interface InsScopeService {

	/**
	 * 拿到所有需要检查的项目
	 * 
	 * @param t
	 * @return
	 */
	public List<InsScope> get(InsTemplate t);

	InsScope findInsScope(Integer id);

	boolean updateInsScope(InsScope insScope);

}
